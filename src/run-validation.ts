/**
 * Validation execution was intentionally removed from the distributable plugin package.
 * Keep this stub so older imports remain source-compatible, but do nothing at runtime.
 */
export function runPackageValidation(_cwd: string): void {
  // no-op by design
}
